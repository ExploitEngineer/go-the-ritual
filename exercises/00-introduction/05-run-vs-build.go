package introduction

func RunVsBuild() {
	// go run main.go - this command will build the code and don't create the executable file just run it directly
	// go build main.go - this command will build the code and generate an executable binary file which we can run
}
